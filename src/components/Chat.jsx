import React from "react";

const Chat = () => {
  return (
    <div style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      height: "80vh",
      background: "linear-gradient(to right, #d4edda, #a3d9a5)",
      color: "#155724",
      fontFamily: "Segoe UI, Tahoma, Geneva, Verdana, sans-serif",
      fontSize: "1.5rem",
      borderRadius: "15px",
      margin: "20px",
      boxShadow: "0 4px 12px rgba(0,0,0,0.1)"
    }}>
      Este apartado está en mantenimiento ⚠️
    </div>
  );
};

export default Chat;
